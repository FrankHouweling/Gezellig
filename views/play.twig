{% include 'base.twig' %}
{%  block pagecontent %}
    <div class="game">
        <ul class="dropzones">
            <li></li>
            <li></li>
            <li></li>
        </ul>

        <ul class="draggable">
            {% for photo in photos %}
                <li>
                    <div class="overlay"></div>
                    <img src="{{ photo }}" />
                    <input type="text" />
                </li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}